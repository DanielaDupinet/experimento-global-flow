import { StyleSheet } from "react-native";

export default StyleSheet.create({
  root: {
    borderWidth: 0,
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "row"
  },
  content: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center"
  },
  disabled: {
    opacity: 0.5
  }
});
